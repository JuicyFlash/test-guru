<h1><%= t('tests.index.header') %></h1>

<table>
  <tr>
    <th><%= t('.test_table_title') %></th>
    <th><%= t('.tests_table_count') %></th>
  </tr>
  <% @tests.each do |test| %>
    <tr>
      <td><%= test.title %></td>
      <td><%= test.questions.count %></td>
      <td><%= button_to t('.start'), start_test_path(test) %></td>
    </tr>
  <% end %>
</table>
<br/>
